/**
 * This is an empty module that gets returned when font files are requested
 * to prevent font loading errors.
 */

// Empty exports to satisfy imports that expect something
export default "";
export const fontData = "";
module.exports = "";
